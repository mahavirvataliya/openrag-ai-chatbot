name: CI

# Lints every PHP file and verifies the autoloader on each push / pull request.
# Keeps the release pipeline green and prevents accidental syntax regressions.

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

permissions:
  contents: read

jobs:
  lint:
    name: PHP lint & autoload check
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: [ '8.0', '8.1', '8.2', '8.3', '8.4' ]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          ini-values: error_reporting=E_ALL, display_errors=On

      - name: Validate composer.json
        run: composer validate --strict --no-check-publish

      - name: Lint all PHP files
        run: |
          set -e
          fail=0
          while IFS= read -r -d '' file; do
            if ! php -l "$file" >/dev/null 2>&1; then
              php -l "$file" || true
              fail=1
            fi
          done < <(find . -name "*.php" -not -path "./vendor/*" -print0)
          exit "$fail"

      - name: Install dependencies (for autoload check)
        run: composer install --no-dev --prefer-dist --no-interaction

      - name: Autoload smoke test
        env:
          OPENRAG_VERSION: '1.0.0'
          OPENRAG_FILE: 'openrag-ai-chatbot.php'
        run: |
          php -r '
          define("ABSPATH", "/tmp/");
          define("OPENRAG_VERSION", "1.0.0");
          define("OPENRAG_FILE", __FILE__);
          define("OPENRAG_DIR", getcwd() . "/");
          define("OPENRAG_URL", "");
          define("OPENRAG_BASENAME", "openrag-ai-chatbot/openrag-ai-chatbot.php");
          define("OPENRAG_DB_VERSION", "1.0.0");
          define("OPENRAG_REST_NAMESPACE", "openrag/v1");
          define("OPENRAG_OPTION_PREFIX", "openrag_");
          require OPENRAG_DIR . "includes/class-autoloader.php";
          OpenRag\Autoloader::register();
          $classes = [
            "OpenRag\\Plugin","OpenRag\\Activator","OpenRag\\Settings",
            "OpenRag\\Database\\Schema",
            "OpenRag\\Embeddings\\Embedding_Manager","OpenRag\\Embeddings\\OpenAI_Embedding",
            "OpenRag\\LLM\\LLM_Manager","OpenRag\\LLM\\OpenAI_LLM","OpenRag\\LLM\\Anthropic_LLM",
            "OpenRag\\VectorStores\\MySQL_Store","OpenRag\\VectorStores\\Cloudflare_Vectorize",
            "OpenRag\\Ingestion\\Ingestion_Pipeline","OpenRag\\Queue\\Background_Processor",
            "OpenRag\\Chat\\Chat_Controller","OpenRag\\MCP\\MCP_Manager",
            "OpenRag\\Admin\\Admin_Menu","OpenRag\\Admin\\Settings_Page",
          ];
          $missing = [];
          foreach ($classes as $c) {
            if (!class_exists($c) && !interface_exists($c)) $missing[] = $c;
          }
          if ($missing) { echo "MISSING:\n - " . implode("\n - ", $missing) . "\n"; exit(1); }
          echo "OK: loaded " . count($classes) . " core classes\n";
          '
